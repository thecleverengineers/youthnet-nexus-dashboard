
{
    "name": "youthnet/api",
    "description": "YouthNet MIS PHP API",
    "type": "project",
    "require": {
        "php": ">=7.4",
        "firebase/php-jwt": "^6.0"
    },
    "autoload": {
        "psr-4": {
            "YouthNet\\": "src/"
        }
    },
    "scripts": {
        "install-dev": [
            "composer install --no-dev --optimize-autoloader"
        ]
    }
}
